/* hw7_6, for loop */
#include <stdio.h>
#include <stdlib.h>
int main(void)
{
   int i,n;
   printf("Please enter a number: "); 
   scanf("%d",&n);
   
   for(i=1;i<=n;i++)
      if (n%i==0)
        printf("%d is a factor of %d\n",i,n);
   
   system("pause");
   return 0;
}
