/* hw8_3, 函數的基本架構 */
#include <stdio.h>
#include <stdlib.h>

int cub(int);

int main(void)
{
    int x=2;
    printf("%d^3=%d\n",x,cub(x));
    
    system("pause");
    return 0;
}

int cub(int x)
{
    return (x)*(x)*(x);
}
