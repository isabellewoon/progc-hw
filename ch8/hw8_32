/* hw8_32, 前置處理器——#define */
#include <stdio.h>
#include <stdlib.h>
#define CUBIC(X) (X)*(X)*(X)

int main(void)
{
    int k1=5;
    double k2=2.4;
    
    printf("%d^3=%d\n",k1,CUBIC(k1));
    printf("%.1lf^3=%lf\n",k2,CUBIC(k2));
    
    system("pause");
    return 0;
}
