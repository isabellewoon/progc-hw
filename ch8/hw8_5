/* hw8_5, 函數的基本架構 */
#include <stdio.h>
#include <stdlib.h>

int mod(int,int);

int main(void)
{
    int x=17, y=5;
    printf("%d/%d的餘數=%d\n",x,y,mod(x,y));
    
    system("pause");
    return 0;
}

int mod(int x,int y)
{
    return (x)%(y);
}
